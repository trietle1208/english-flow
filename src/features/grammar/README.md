# features/grammar

Grammar topic catalog and detail pages (spec §17 / Phase 10).

- `queries.ts` — list topics (completion from `quiz_attempts`) and topic detail
- `components/` — cards, examples toggle, common mistakes, topic content + footer nav
- Mini quiz uses shared `QuizRunner` from `features/quiz`
